{
  "buildCommand": "npx prisma generate",
  "functions": {
    "api/[...path].ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/api/[...path]"
    }
  ]
}

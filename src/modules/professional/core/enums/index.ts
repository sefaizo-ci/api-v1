export enum ProfessionalStatus {
  PENDING = 'PENDING',
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED',
  REJECTED = 'REJECTED',
}

export enum ServiceLocation {
  HOME = 'HOME',
  SALON = 'SALON',
  BOTH = 'BOTH',
}

export enum AvailabilityStatus {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED',
  ON_LEAVE = 'ON_LEAVE',
}
